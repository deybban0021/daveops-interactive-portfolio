# DaveOps Portfolio Restart

This repository has been reset to a minimal Vite + React foundation.

Current scope:

- Keep `DESIGN.md` as the design source of truth
- Maintain the existing GitHub repository and `main` branch
- Rebuild the portfolio from this clean starter

Available scripts:

- `npm run dev`
- `npm run build`
- `npm run preview`
